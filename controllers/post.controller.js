import {primsa} from '../prisma/index.js';

const createPost = async(req, res) => {
    try {
        const user = req.user;
        const {title, body, slug, author}
    } catch (error) {
        
    }
}